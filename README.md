# LTS Health

Dedicated frontend repository for LTS Health.

Health data is stored in the dedicated Supabase project and must never be committed to this repository. The frontend uses only a publishable client key with Row Level Security enforced in the backend.
