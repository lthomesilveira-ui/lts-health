name: LTS Health Smoke
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: JavaScript syntax
        run: |
          node --check app.js
          node --check app-v8.js
          node --check app-v11.js
          node --check app-v12.js
          node --check app-v13.js
          node --check app-v14.js
          node --check app-v15.js
          node --check app-v16.js
          node --check app-v17.js
          node --check app-v18.js
          node --check app-v19.js
          node --check sw.js
          node --check config.js
      - name: Dedicated backend guardrails
        run: |
          grep -q "plztdqyuqcjohiimudnr" config.js
          grep -q "health-inspect-upload-v2" config.js
          ! grep -R "tadhkamnwtsbdozwkyut" --exclude-dir=.git --exclude=smoke.yml --include='*.js' --include='*.html' --include='*.css' --include='*.md' .
      - name: Product layer references
        run: |
          test -f v10.css
          test -f v11.css
          test -f v12.css
          test -f v13.css
          test -f v14.css
          test -f v15.css
          test -f v16.css
          test -f v17.css
          test -f v18.css
          test -f v19.css
          test -f app-v11.js
          test -f app-v12.js
          test -f app-v13.js
          test -f app-v14.js
          test -f app-v15.js
          test -f app-v16.js
          test -f app-v17.js
          test -f app-v18.js
          test -f app-v19.js
          test -f manifest.webmanifest
          test -f sw.js
          grep -q "v17.css" config.js
          grep -q "v18.css" config.js
          grep -q "v19.css" config.js
          grep -q "app-v17.js" config.js
          grep -q "app-v18.js" config.js
          grep -q "app-v19.js" config.js
          ! grep -q "app-v11.js" config.js
          ! grep -q "app-v12.js" config.js
          ! grep -q "app-v13.js" config.js
          ! grep -q "app-v14.js" config.js
          ! grep -q "app-v15.js" config.js
          ! grep -q "app-v16.js" config.js
          grep -q "manifest.webmanifest" app-v13.js
          grep -q "supabase.co" sw.js
          grep -q "health_body_composition" app-v14.js
          grep -q "health_workouts" app-v14.js
          grep -q "health_medication_events" app-v15.js
          grep -q "Esta tela não mostra doses" app-v15.js
          grep -q "Peso · massa muscular · gordura" app-v16.js
          grep -q "Fonte bruta" app-v16.js
          grep -q "Seu histórico, sem ruído" app-v17.js
          grep -q "Buscar no histórico" app-v17.js
          grep -q "Apple Health" app-v17.js
          grep -q "Auditoria e base canônica" app-v18.js
          grep -q "O que mudou recentemente" app-v18.js
          grep -q "Ritmo de treino e histórico por exercício" app-v19.js
          grep -q "Cobertura nutricional por semana" app-v19.js
      - name: No private health payloads in repository
        run: |
          ! find . -type f \( -name '*.zip' -o -name '*.pdf' -o -name '*.csv' -o -name '*.json' \) -not -path './.git/*' | grep .
