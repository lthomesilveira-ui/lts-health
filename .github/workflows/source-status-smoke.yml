name: LTS Health v2 Source Status Smoke
on:
  push:
    branches: [architecture-v2]
    paths:
      - 'v2/src/source-status.js'
      - 'v2/src/data-screen.js'
      - 'v2/source-status-smoke.mjs'
      - '.github/workflows/source-status-smoke.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  source-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static source contract
        run: |
          node --check v2/src/source-status.js
          node --check v2/source-status-smoke.mjs
          grep -Fq "export const stableAppleMetricTypes=new Set(['active_energy_kcal','exercise_minutes','stand_hours']);" v2/src/source-status.js
          grep -Fq "const preservedCandidateStatuses=new Set(['candidate','held']);" v2/src/source-status.js
          grep -q "isPreservedCandidate" v2/src/source-status.js
          grep -q "candidateFromFamily(sourceMetrics,'polar_flow')" v2/src/source-status.js
          grep -q "candidateFromFamily(sourceMetrics,'myfitnesspal')" v2/src/source-status.js
          grep -q "latestSourceMetricDateFor" v2/src/source-status.js
          ! grep -q "stableAppleMetricTypes.*sleep_duration_h" v2/src/source-status.js
      - name: Install browser runtime
        run: |
          npm init -y >/dev/null 2>&1
          npm install --no-save playwright@1.55.0
          npx playwright install chromium
      - name: Source status desktop and mobile
        run: |
          python3 -m http.server 4173 --directory v2 >/tmp/lts-health-v2-http.log 2>&1 & SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          sleep 2
          node v2/source-status-smoke.mjs
